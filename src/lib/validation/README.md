# Validation

Zod schemas for every external input: student signup fields, staff pickup
codes and bin numbers, admin configuration, and inbound SMS commands. Server
operations must parse input through these schemas before touching data.
